{
  "default": {
    "paths": ["features/**/*.feature"],
    "require": ["support/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["summary", "html:test-reports/test-report.html"]
  }
}
